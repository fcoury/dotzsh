[user]
	name = Felipe Coury
	email = felipe.coury@gmail.com
	signingkey = 163F83077834A917

[github]
	user = fcoury

[core]
	editor = code --wait
	#pager = less -F -X
	pager = delta

[interactive]
	diffFilter = delta --color-only

[color]
	ui = true

[push]
	default = simple
	autoSetupRemote = true

[alias]
	st = status -sb
	co = checkout

[pull]
	rebase = false

[init]
	defaultBranch = master

[diff]
	colorMoved = default
	external = difft

[diff "hex"]
	textconv = hexdump -v -C

[delta]
	navigate = true

[merge]
	conflictstyle = diff3

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
